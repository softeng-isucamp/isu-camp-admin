[pytest]
# Tests use plain module names (app/tests/actions.py), so pytest must be told
# to collect every .py under app/tests and to stay out of the source packages.
testpaths = app/tests
python_files = *.py
norecursedirs = routes services model algorithm frontend venv node_modules build dist .* *.egg
